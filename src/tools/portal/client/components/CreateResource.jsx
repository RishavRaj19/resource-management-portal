import { Typography } from "@material-ui/core"

export const CreateResource = () => {

    return (
        <div style={{margin: 20}}>
            <Typography variant={'h4'} align={'center'}> Create Resource </Typography>
        </div>
    )
}